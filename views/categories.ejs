<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      type="text/css"
      href="../public/styles/general.css"
    />
  </head>
  <body>
    <%- include('navbar') %>

    <main>
      <p>Welcome to all categories page</p>

      <% if (categories.length > 0) { %> <% categories.forEach(({name,
      imagepath}) => { %>
      <p><%=imagepath%></p>
      <img src="../<%= imagepath %>" alt="image" />
      <br />
      <%})%> <% } %>
    </main>
  </body>
</html>
